{
  "pluginAlias": "dynamicAPI",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "remoteApiDisplayName": {
        "title": "Name",
        "type": "string",
        "default": "Remote API",
        "description": "The display name of the remote API"
      },
      "remoteApiURL": {
        "title": "Host",
        "type": "string",
        "required": true,
        "default": "<host>:<port>/<endpoint>/"
      },
      "directConnectApiIP": {
        "title": "Direct Connect API IP Address",
        "type": "string",
        "default": "",
        "format": "ipv4",
        "description": "Force the IP address used for the Direct Connect API - Leave blank for automatic assignment of IP address"
      },
      "directConnectApiPort": {
        "title": "Direct Connect API Port",
        "type": "integer",
        "required": true,
        "default": "8001",
        "description": "The Port of Direct Connect API"
      },
      "https": {
        "title": "Enable HTTPS",
        "type": "boolean",
        "default": false
      },
      "rejectInvalidCert": {
        "title": "Reject Invalid Certificate",
        "type": "boolean",
        "default": true,
        "description": "Terminate the HTTPS connection if the Remote API presents an invalid certificate"
      },
      "httpsCertPath": {
        "title": "HTTPS Certificate",
        "type": "string",
        "required": true,
        "default": "/<certificate path>/<certificate>",
        "description": "Certificate for HTTPS Direct Connect Service"
      },
      "httpsKeyPath": {
        "title": "HTTPS Private Key",
        "type": "string",
        "required": true,
        "default": "/<private key path>/<private key>",
        "description": "Private Key for HTTPS Direct Connect Service"
      },
      "jwt": {
        "title": "Enable JSON Web Token (JWT) Secure API",
        "type": "boolean",
        "default": false
      },
      "jwtAudience": {
        "title": "JWT Audience",
        "type": "string",
        "required": true,
        "default": "https://<JWT API Application>/",
        "format": "uri",
        "description": "The JWT Audience for the Direct Connect & Remote APIs"
      },
      "jwtIssuer": {
        "title": "JWT Issuer",
        "type": "string",
        "required": true,
        "default": "https://<JWT Issuer>/",
        "format": "uri",
        "description": "The JWT Issuer for the Direct Connect & Remote APIs"
      },
      "jwtClientID": {
        "title": "JWT Client ID",
        "type": "string",
        "required": true,
        "default": "<JWT Client ID>",
        "description": "The JWT Client ID for the Direct Connect & Remote APIs"
      },
      "jwtClientSecret": {
        "title": "JWT Client Secret",
        "type": "string",
        "required": true,
        "default": "<JWT Client Secret>",
        "description": "The JWT Client Secret for the Direct Connect & Remote APIs"
      }
    }
  },
  "layout":[
  {
    "key": "RemoteAPI",
    "type": "section",
    "title": "Remote API",
    "expandable": false,
    "expanded": true,
    "items": [
      "remoteApiDisplayName",
      "remoteApiURL"
      ]
  },
  {
    "key": "DirectConnectAPI",
    "type": "section",
    "title": "Direct Connect API",
    "expandable": true,
    "expanded": false,
    "items": [
      "directConnectApiIP",
      "directConnectApiPort"
      ]
  },
  {
    "key": "https",
    "type": "section",
    "title": "HTTPS",
    "expandable": true,
    "expanded": false,
    "items": [
      "https",
      "rejectInvalidCert",
      "httpsCertPath",
      "httpsKeyPath"
      ]
  },
  {  
    "key": "jwt",
    "type": "section",
    "title": "JSON Web Token (JWT) Secure API",
    "expandable": true,
    "expanded": false,
    "items": [
      "jwt",
      "jwtAudience",
      "jwtIssuer",
      "jwtClientID",
      "jwtClientSecret"
      ]
    }
   ]
}
